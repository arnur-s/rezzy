# Sibling agent worktrees — `pnpm format` would rewrite another agent's files.
.claude/worktrees/

package-lock.json
pnpm-lock.yaml
yarn.lock

# Output of `pnpm theme:build` (astryx theme build) — generated, not edited.
src/themes/**/*.js
src/themes/**/*.d.ts
src/themes/**/theme.css